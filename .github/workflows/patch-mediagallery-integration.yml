name: Patch MediaGallery integration

on:
  push:
    branches: [modernize-1.1.10]

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo patch-ready
